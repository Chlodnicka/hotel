<%= form_for(room_type_of_room) do |f| %>
  <% if room_type_of_room.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(room_type_of_room.errors.count, "error") %> prohibited this room_type_of_room from being saved:</h2>

      <ul>
      <% room_type_of_room.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :room_id %>
    <%= f.text_field :room_id %>
  </div>

  <div class="field">
    <%= f.label :type_of_room_id %>
    <%= f.text_field :type_of_room_id %>
  </div>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
