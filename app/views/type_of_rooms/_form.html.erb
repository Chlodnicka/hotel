<%= form_for(type_of_room) do |f| %>
  <% if type_of_room.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(type_of_room.errors.count, "error") %> prohibited this type_of_room from being saved:</h2>

      <ul>
      <% type_of_room.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %>
    <%= f.text_field :name %>
  </div>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
